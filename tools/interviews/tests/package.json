{
  "name": "ed-interviews-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore security-rule tests for the ED interviews tool. Dev-only — the tool itself has no build step and no dependencies.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project mgh-ed-hiring-test \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0",
    "firebase-tools": "^13.29.1"
  }
}
